apiVersion: v1
kind: Service
metadata:
  name: svc-produto
  labels:
    app: produto
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
  selector:
    app: produto